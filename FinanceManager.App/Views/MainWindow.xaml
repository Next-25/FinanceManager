<Window x:Class="FinanceManager.App.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="clr-namespace:FinanceManager.App.ViewModels"
        Title="Финансовый менеджер" Height="600" Width="1100"
        WindowStartupLocation="CenterScreen"
        Icon="/Images/Icon.png" FontFamily="Yu Gothic UI" FontSize="13">
    <Window.Resources>
        <Style x:Key="FocusVisual">
            <Setter Property="Control.Template">
                <Setter.Value>
                    <ControlTemplate>
                        <Rectangle Margin="2" StrokeDashArray="1 2" Stroke="{DynamicResource {x:Static SystemColors.ControlTextBrushKey}}" SnapsToDevicePixels="true" StrokeThickness="1"/>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <SolidColorBrush x:Key="Button.Static.Background" Color="#FF0021D6"/>
        <SolidColorBrush x:Key="Button.Static.Border" Color="#FF707070"/>
        <SolidColorBrush x:Key="Button.MouseOver.Background" Color="#FF26BDDA"/>
        <SolidColorBrush x:Key="Button.MouseOver.Border" Color="#FF3C7FB1"/>
        <SolidColorBrush x:Key="Button.Pressed.Background" Color="#FFC4E5F6"/>
        <SolidColorBrush x:Key="Button.Pressed.Border" Color="#FF2C628B"/>
        <SolidColorBrush x:Key="Button.Disabled.Background" Color="#FFF4F4F4"/>
        <SolidColorBrush x:Key="Button.Disabled.Border" Color="#FFADB2B5"/>
        <SolidColorBrush x:Key="Button.Disabled.Foreground" Color="#FF838383"/>
        <Style x:Key="ButtonStyle1" TargetType="{x:Type Button}">
            <Setter Property="FocusVisualStyle" Value="{StaticResource FocusVisual}"/>
            <Setter Property="Background" Value="{StaticResource Button.Static.Background}"/>
            <Setter Property="BorderBrush" Value="{StaticResource Button.Static.Border}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Padding" Value="10, 5, 10, 5"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Button}">
                        <Border x:Name="border" CornerRadius="7" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" SnapsToDevicePixels="true">
                            <ContentPresenter x:Name="contentPresenter" Focusable="False" HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" Margin="{TemplateBinding Padding}" RecognizesAccessKey="True" SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsDefaulted" Value="true">
                                <Setter Property="BorderBrush" TargetName="border" Value="{DynamicResource {x:Static SystemColors.HighlightBrushKey}}"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="true">
                                <Setter Property="Background" TargetName="border" Value="{StaticResource Button.MouseOver.Background}"/>
                                <Setter Property="BorderBrush" TargetName="border" Value="{StaticResource Button.MouseOver.Border}"/>
                                <Setter Property="TextElement.Foreground" TargetName="contentPresenter" Value="Black"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="true">
                                <Setter Property="Background" TargetName="border" Value="{StaticResource Button.Pressed.Background}"/>
                                <Setter Property="BorderBrush" TargetName="border" Value="{StaticResource Button.Pressed.Border}"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="false">
                                <Setter Property="Background" TargetName="border" Value="{StaticResource Button.Disabled.Background}"/>
                                <Setter Property="BorderBrush" TargetName="border" Value="{StaticResource Button.Disabled.Border}"/>
                                <Setter Property="TextElement.Foreground" TargetName="contentPresenter" Value="{StaticResource Button.Disabled.Foreground}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    
    <Window.DataContext>
        <vm:MainViewModel/>
    </Window.DataContext>

    <Window.InputBindings>
        <KeyBinding Key="OemPlus" Command="{Binding AddIncomeCommand}" />
        <KeyBinding Key="Add" Command="{Binding AddIncomeCommand}" />

        <KeyBinding Key="OemMinus" Command="{Binding AddExpenseCommand}" />
        <KeyBinding Key="Subtract" Command="{Binding AddExpenseCommand}" />
    </Window.InputBindings>

    <DockPanel>
        <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="10" HorizontalAlignment="Center">
            <Border Background="#FF0EFF1D" Height="50" Margin="0,0,20,0" CornerRadius="10,10,10,10" Padding="10" Width="270">
                <TextBlock Text="{Binding IncomeSum, StringFormat=Поступления: {0:C}, ConverterCulture=ru-RU}" 
                FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center"/>                
            </Border>
            <Border Background="#FFF34714" Height="50" Margin="0,0,20,0" CornerRadius="10,10,10,10" Padding="10" Width="270">
            <TextBlock Text="{Binding ExpenseSum, StringFormat=Расход: {0:C}, ConverterCulture=ru-RU}" 
               FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center"/>                
            </Border>
            <Border Background="#FF26BDDA" Height="50" Margin="0,0,20,0" CornerRadius="10,10,10,10" Padding="10" Width="270">
            <TextBlock Text="{Binding Balance, StringFormat=Остаток: {0:C}, ConverterCulture=ru-RU}" 
               FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center"/>                
            </Border>
        </StackPanel>
        
        <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="10" VerticalAlignment="Center" HorizontalAlignment="Center">
            <TextBlock Text="С:" Margin="0,0,6,0" VerticalAlignment="Center"/>
            <DatePicker SelectedDate="{Binding From, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Width="140" Margin="0,0,10,0" VerticalContentAlignment="Center"/>
            <TextBlock Text="По:" Margin="0,0,6,0" VerticalAlignment="Center"/>
            <DatePicker SelectedDate="{Binding To, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Width="140" Margin="0,0,10,0" VerticalContentAlignment="Center"/>
            
            <Button Style="{DynamicResource ButtonStyle1}" Content="Сбросить фильтр" Command="{Binding ClearFilterCommand}"/>                
            
        </StackPanel>


        <StackPanel DockPanel.Dock="Left" VerticalAlignment="Top" Margin="0,10,0,0">
            <Button Style="{DynamicResource ButtonStyle1}" Content="Поступления (+)" Command="{Binding AddIncomeCommand}" Margin="6,0,0,10" Padding="0,5,0,5" Width="110"/>
            <Button Style="{DynamicResource ButtonStyle1}" Content="Расход (-)" Command="{Binding AddExpenseCommand}" Margin="6,0,0,10" Padding="0,5,0,5" Width="110"/>
            <Button Style="{DynamicResource ButtonStyle1}" Content="Изменить" Command="{Binding EditCommand}" Margin="6,0,0,10" Padding="0,5,0,5" Width="110"/>
            <Button Style="{DynamicResource ButtonStyle1}" Content="Удалить" Command="{Binding DeleteCommand}" Margin="6,0,0,0" Padding="0,5,0,5" Width="110"/>
        </StackPanel>

        <DataGrid ItemsSource="{Binding Transactions}" SelectedItem="{Binding Selected}"
                  AutoGenerateColumns="False" Margin="10" IsReadOnly="True" MouseDoubleClick="DataGrid_MouseDoubleClick" BorderBrush="{x:Null}" Background="{x:Null}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Дата" Binding="{Binding Date, StringFormat={}{0:dd.MM.yyyy}}" Width="120"/>
                <DataGridTextColumn Header="Тип" Binding="{Binding Type}" Width="100"/>
                <DataGridTextColumn Header="Категория" Binding="{Binding Category}" Width="150"/>
                <DataGridTemplateColumn Header="Сумма" Width="120">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding Amount, StringFormat={}{0:C}, ConverterCulture=ru-RU}">
                                <TextBlock.Style>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="Foreground" Value="Black"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding Type}" Value="Поступление">
                                                <Setter Property="Foreground" Value="#FF1FB91F"/>
                                                <Setter Property="FontWeight" Value="Bold"/>
                                            </DataTrigger>                                            
                                        </Style.Triggers>
                                    </Style>
                                </TextBlock.Style>
                            </TextBlock>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                <DataGridTextColumn Header="Описание" Binding="{Binding Description}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>
        <Button Content="Button" Width="NaN"/>
    </DockPanel>
</Window>
